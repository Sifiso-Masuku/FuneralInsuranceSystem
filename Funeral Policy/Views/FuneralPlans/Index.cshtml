@model IEnumerable<Funeral_Policy.Models.FuneralPlan>

@{
    ViewBag.Title = "Index";
}

<h2>Funeral Plan List</h2>

<p>
    @Html.ActionLink("Add New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FuneralPlanName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FuneralCoverPayout.PayoutAmount)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.FuneralPlanDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.FuneralPlanName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.FuneralCoverPayout.PayoutAmount)
    </td>


    <td>
        @Html.DisplayFor(modelItem => item.FuneralPlanDescription)
    </td>
    <td>

        @*@Html.ActionLink("Edit", "Edit", new { id=item.FuneralPlanId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FuneralPlanId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FuneralPlanId })*@
        <a href="@Url.Action("Details","FuneralPlans", new { id=item.FuneralPlanId})" rel="tooltip" title="View" class="btn btn-info btn-simple btn-xs">Details<i class="fa fa-eye"></i></a>
        <a href="@Url.Action("Edit","FuneralPlans", new { id=item.FuneralPlanId })" rel="tooltip" title="Edit" class="btn btn-success btn-simple btn-xs">Edit<i class="fa fa-edit"></i></a>
        <a href="@Url.Action("Delete", "FuneralPlans", new { id = item.FuneralPlanId })" rel="tooltip" title="Remove" class="btn btn-danger btn-simple btn-xs">Delete<i class="fa fa-times"></i></a>

    </td>
</tr>
}

</table>
